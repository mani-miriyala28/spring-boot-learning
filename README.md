# Modern Spring Learning Workspace

Each folder is an independent Spring Boot application with its own:
- pom.xml
- Application class
- application.properties

Run each module independently from your IDE.
